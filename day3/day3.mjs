import {FilePaths} from './util.mjs';
import {readFile} from '../util.mjs'

// ------------------- PROLOGUE ------------------- //

// Read in the file, split it up into each new line, and intialize the array for the numbers of each line
const fileContents = readFile(FilePaths.ACTUAL_INPUT);

// ------------------- PROLOGUE ------------------- //
